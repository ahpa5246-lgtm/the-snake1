services:
  - type: web
    name: the-snake1
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: BATTLESNAKE_REPLAY_DIR
        value: training/live_games
