# Battlesnake Blackout 2026 — Python dependencies

# ASGI framework + routing
fastapi>=0.111.0

# Production ASGI server
uvicorn[standard]>=0.29.0

# Data validation (FastAPI dependency)
pydantic>=2.7.0

# CPU neural inference for the automatically promoted champion.
# Pin the CPU wheel explicitly.  The default Linux wheel now pulls several GB
# of CUDA libraries even though Render and the GitHub learner both run on CPU.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu
