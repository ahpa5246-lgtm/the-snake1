# Training imports the tactical FastAPI module for its safety mask and teacher
# policy, so the production dependencies are required too.  requirements.txt
# also selects the CPU-only PyTorch wheel used by GitHub-hosted runners.
-r requirements.txt
numpy>=1.26.0
hisss>=0.1.0
